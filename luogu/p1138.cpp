#include <iostream>
#include <algorithm>

using namespace std;

int main(int argc, char const *argv[])
{
	int n;
	int k;
	cin >> n >> k;

	int num[n];
	for (int i = 0; i < n; ++i)
	{
		cin >> num[i];
	}

	sort(num,num+n);

	for (int i = 0; i != k; ;)
	{
		if (num[i] == )
		{
			/* code */
		}
	}

	return 0;
}