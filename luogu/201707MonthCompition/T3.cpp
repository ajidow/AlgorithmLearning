#include <iostream>

using namespace std;

typedef struct HouseAndPeople
{
	long long x;
	long long r;	
}HAP;

int main(int argc, char const *argv[])
{
	//

	long long L,N;
	cin >> L >> N;

	//

	HAP inportant[N];
	long long i;
	for (i = 0; i < N; ++i)
	{
		cin >> inportant[i].x >> inportant[i].r;
	}

	//

	long long Length[N];

	for (i = 0; i < N; ++i)
	{
		
	}

	return 0;
}